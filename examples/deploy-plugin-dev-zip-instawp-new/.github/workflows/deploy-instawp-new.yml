name: Deploy Plugin Dev Zip to New InstaWP Site

on:
  workflow_dispatch:
    inputs:
      instawp_url:
        required: false
        type: string
        default: ""
        description: "Desired name for the new InstaWP site (e.g. my-plugin-test). Auto-generated if omitted."

jobs:
  deploy:
    uses: krokedil/krokedil-wp-ci/.github/workflows/deploy-plugin-dev-zip-instawp-new.yml@v1
    with:
      instawp_url: ${{ inputs.instawp_url }}
    secrets:
      INSTAWP_API_TOKEN: ${{ secrets.INSTAWP_API_TOKEN }}
      E2E_API_KEY: ${{ secrets.E2E_API_KEY }}
      E2E_API_SECRET: ${{ secrets.E2E_API_SECRET }}
